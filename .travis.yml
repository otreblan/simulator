sudo: required


services:
    - docker
script:
    - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
    - docker build . -t arch
    - docker run --rm -v $(pwd):/home/docker/build arch -c "makepkg -s --noconfirm"
